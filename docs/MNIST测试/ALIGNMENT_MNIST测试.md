# MNIST测试集运行对齐文档

## 1. 项目上下文分析

### 项目现状
- **当前环境**: 已完成FedGMM在FEMNIST上的成功验证
- **技术栈**: PyTorch 2.6.0+cpu, Python 3.13.0, 完整虚拟环境
- **框架状态**: FedGMM联邦学习框架运行正常
- **经验积累**: 具备跨数据集适配和问题解决经验

### MNIST数据集支持分析
**发现的MNIST支持**:
- ✅ **mnist**: 标准MNIST数据集支持
- ✅ **mnist9**: MNIST变体(可能是OOD相关)
- ✅ **SubMNIST**: 专用的MNIST数据集类
- ✅ **get_mnist()**: 数据获取函数
- ✅ **数据目录**: data/mnist/已存在

**技术规格**:
- **数据格式**: .pkl文件 (与FEMNIST的.pt不同)
- **图像尺寸**: 28x28灰度图像
- **加载器类型**: "mnist" 
- **聚合器**: 可复用ACGcentralized
- **客户端**: 可复用ACGmixture

### 架构兼容性
- ✅ **数据加载**: 支持SubMNIST数据集类
- ✅ **模型架构**: 28x28输入已在FEMNIST验证
- ✅ **ResNet适配**: 已有28x28→32x32转换逻辑
- ✅ **联邦框架**: FedGMM核心架构无需修改

## 2. 需求理解确认

### 原始需求
"按照6A流程, 运行mnist测试集"

### 需求解读
用户希望在MNIST数据集上运行FedGMM联邦学习实验，类似之前在FEMNIST上的成功验证。

### 边界确认
**明确的边界**:
- 基于现有FedGMM框架和成功的FEMNIST经验
- 使用MNIST数据集(标准手写数字识别)
- 快速验证导向，复用10轮训练配置
- CPU环境，保持兼容性优先

**需要确认的边界**:
1. **MNIST变体选择**: "mnist" vs "mnist9" 哪个更合适？
2. **实验规模**: 继续快速验证(10轮)还是完整训练？
3. **数据准备**: MNIST数据是否需要预处理？
4. **对比目的**: 是验证功能还是与FEMNIST结果对比？

### 智能决策策略

#### 基于项目知识的推理
1. **选择标准MNIST**:
   - "mnist"是标准版本，更具代表性
   - "mnist9"似乎是OOD检测相关的特定变体
   - 标准MNIST便于与其他研究对比

2. **快速验证策略**:
   - 基于成功的FEMNIST经验，10轮训练已证明有效
   - 快速验证可以及时发现问题
   - 用户要求"测试集"暗示验证性质

3. **复用成功配置**:
   - FEMNIST的参数配置已验证有效
   - 28x28灰度图像格式相同
   - ResNet适配逻辑可直接复用

### 疑问澄清

#### 需要确认的关键点
1. **数据集选择**: 确认使用标准"mnist"而非"mnist9"
2. **数据状态**: 检查data/mnist/目录的数据准备情况
3. **参数复用**: 确认使用与FEMNIST相同的快速验证配置

## 3. 技术实现方案预估

### 预期的技术方案
```bash
# 预估的运行命令
python run_experiment.py mnist FedGMM \
    --n_learners 3 \
    --n_gmm 3 \
    --n_rounds 10 \
    --bz 64 \
    --lr 0.01 \
    --lr_scheduler multi_step \
    --log_freq 2 \
    --device cpu \
    --optimizer sgd \
    --seed 1234 \
    --logs_dir ./logs \
    --verbose 1
```

### 预期的适配需求
- ✅ **最小修改**: 数据格式兼容，主要是数据集名称切换
- ✅ **图像处理**: 复用现有的28x28→32x32转换逻辑
- ✅ **联邦架构**: 无需修改，直接复用

### 风险评估
- **低风险**: MNIST是最标准的数据集，支持完善
- **低风险**: 28x28灰度图像与FEMNIST格式相同
- **低风险**: 项目原生支持MNIST数据集

## 4. 验收标准预估

### 功能验收预期
1. **数据加载**: MNIST数据正确加载和分割
2. **训练执行**: 10轮FedGMM训练正常完成
3. **结果输出**: 获得训练/测试准确率和损失
4. **日志记录**: TensorBoard日志正常生成

### 性能验收预期  
- **运行时间**: 预计与FEMNIST相似(4-6分钟)
- **准确率**: 期望高于FEMNIST(MNIST相对简单)
- **收敛性**: 训练损失正常下降

### 对比价值
- **数据集对比**: MNIST vs FEMNIST性能差异
- **算法验证**: FedGMM在不同手写识别数据集上的泛化能力
- **框架稳定性**: 验证跨数据集的架构稳定性

## 5. 项目特性规范对齐

### 代码风格对齐
- ✅ 复用现有的run_experiment.py框架
- ✅ 保持与FEMNIST实验相同的配置风格
- ✅ 维护现有的日志和保存格式

### 架构对齐
- ✅ 使用相同的ACGCentralizedAggregator
- ✅ 使用相同的ACGMixtureClient
- ✅ 保持相同的TensorBoard集成

### 质量对齐
- ✅ 遵循已验证的10轮快速验证策略
- ✅ 保持相同的错误处理模式
- ✅ 生成相同格式的文档和报告

## 6. 执行计划预览

### 预期任务序列
1. **T1: MNIST数据验证** - 检查data/mnist/状态
2. **T2: 参数配置适配** - 调整为mnist数据集
3. **T3: FedGMM训练执行** - 运行10轮训练
4. **T4: 结果收集分析** - 收集性能指标
5. **T5: 对比报告生成** - 与FEMNIST结果对比
6. **T6: 文档总结** - 生成MNIST验证报告

### 预计时间
- **总时间**: 15-20分钟 (包括数据检查)
- **训练时间**: 5-8分钟 (预计比FEMNIST稍快)
- **文档时间**: 5-10分钟

这个对齐分析为MNIST测试集运行提供了完整的上下文理解和执行规划基础。